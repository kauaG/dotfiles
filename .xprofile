#!/bin/sh

xrdb $HOME/.config/Xresources & # Use Xresources colors/settings on startup
mpd &			# music player daemon
wall &			# set the wallpaper with the `wall` script
dunst &			# dunst for notifications
xset r rate 300 50 &	# Speed xrate up
